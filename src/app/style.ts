import { css } from '@emotion/css';

export const cssFullHeightAndWidth = css({
  height: '100%',
  width: '100%',
});

export const cssBody = css({
  margin: 0,
  height: '100%',
  width: '100%',
});
