import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 12,
    // colorPrimary: '#52c41a',

    // Seed Token
    // colorPrimary: '#00b96b',
    borderRadius: 4,

    // Alias Token
    // colorBgContainer: '#f6ffed',
  },
};

export default theme;
